fn main() {
    let mut counter: u32 = 1;
    let mut run: bool = true;

    // initialise all components
    
    while run {
        // update

        // actions

        // counter update

        // if program must keep running
    }

    // free all components
}
